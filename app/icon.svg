<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
  <!--
    BUYER: REPLACE THIS, and keep it in step with components/brand/Logo.tsx.

    Same mark as the header logo. A favicon is fetched outside the page, so it
    cannot read --accent from globals.css — the accent is written literally
    here. Change it in both places, or the tab icon drifts from the header.
  -->
  <rect width="32" height="32" rx="8" fill="#5b4be8"/>
  <path d="M16 7.5 L24.5 23 L16 18.6 L7.5 23 Z" fill="#ffffff"/>
</svg>
